var newArray = [];

function map(array, elm) {
  array.forEach(function(element) {
    newArray.push(elm
  (element));
  });

  console.log(newArray);
}
  function elm(index) {
    return index.length;
  }

  map(["whats", "up", "my", "dudes"], elm
);